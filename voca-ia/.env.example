# Chave da API da Anthropic — usada SOMENTE pelo servidor (server/index.js).
# Sem ela o app continua funcionando inteiro no modo offline (Grimm roteirizado).
ANTHROPIC_API_KEY=coloque-sua-chave-aqui

# Porta do servidor (opcional, padrao 8787)
PORT=8787
